export const environment = {
  production: true,
  /**
   * Empty means same-origin: serve the built UI behind the same host as
   * Workvivo.API, or put a reverse proxy in front of both.
   */
  apiBaseUrl: '',
};
