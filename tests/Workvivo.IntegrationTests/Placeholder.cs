namespace Workvivo.IntegrationTests;

/// <summary>
/// Placeholder so the project compiles before the authentication phase adds the
/// WebApplicationFactory fixture and the first suites. See README.md.
/// </summary>
internal static class Placeholder;
